{
  "name": "hfjp",
  "private": true,
  "scripts": {
    "clean": "rimraf .turbo pnpm-lock.yaml node_modules && turbo run clean",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "rimraf": "6.1.0",
    "prettier": "3.6.2",
    "turbo": "2.6.0",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
